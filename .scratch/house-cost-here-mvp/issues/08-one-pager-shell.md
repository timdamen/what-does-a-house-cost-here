# 08 One-Pager shell and data loading

Status: done
Type: task
Blocked by: 05, 06, 07

## Goal

Assemble the page: location flow, map, and the data plumbing that the list (09) and facts (10) tickets consume. User stories 21, 22, 39, 40, 41, 46.

## Scope

- `app/pages/index.vue`: server-rendered shell. With no Location: `LocationPrompt`. With a Location: header bar (app name, `PlaceSearch` trigger, `LocateMeButton`), `HouseMap` (client-only, lazy), then two named regions `<section id="houses">` and `<section id="neighbourhood">` with skeletons that tickets 09 and 10 fill. Wide screens (research breakpoint): map left, sections right.
- `app/composables/useHouses.ts`, `useFacts.ts`, `usePrices.ts`: `useFetch`/`useAsyncData` against the routes, keyed on rounded lat/lng/radius, with a client-side cache (Nuxt payload plus `useNuxtData` or a small module-level `Map`) so revisiting a recent area is instant. Prices are fetched after houses and merged into `House.priceSignal`.
- `app/composables/useLocaleFormat.ts`: currency and distance formatting derived from `facts.hierarchy.countryCode` (metric vs imperial for `US`, `GB` miles for long distances; use `Intl`). Wraps the domain helpers.
- Error state: an `UpstreamError` response renders a `ErrorRetry.vue` card ("Open data service unavailable, it's them not you") with a retry button; never a blank page.
- Loading: skeletons for map, list and facts; the page is interactive before the map script loads.
- Wire `search-here`, `radius-change`, `select` from the map to `useLocation`.

## Acceptance

- Manual check with the fixture provider: prompt, map, skeleton sections, retry state (use the failing provider switch from ticket 05).
- `pnpm quality` green.

## Updates after research (ticket 01)

Overturned default 2 in `docs/research/mobile-map-ux.md` changes the layout: on phones the map fills the viewport and a `BottomSheet.vue` (custom, SSR-rendered, three snap points peek/half/full per decision 2, `dvh` units, drag handle that also cycles snap points on tap, keyboard operable, `0ms` transitions under reduced motion, Back/Escape collapses from full) hosts the two sections `#houses` and `#neighbourhood` and, above them, a slot for the selected House card. At `>= 840px` it renders as a side panel one third wide with the map beside it. Build `BottomSheet.vue` in this ticket with the sections as skeleton content; tickets 09 and 10 fill the sections and 09 adds the card. Keep the header bar (search, locate) at the top under `env(safe-area-inset-top)`. The `#fallback` map placeholder and the sheet must give a stable layout (CLS ≤ 0.1).

## Follow-up from ticket 06

`PlaceSearch.vue` currently calls `/api/geocode` on every debounced keystroke. Nominatim's usage policy forbids autocomplete, so change it to search only on submit (Enter key or a search button) and keep results shown until the query changes. Update its unit test accordingly.

## Comments

- Done. `pnpm quality` green (fmt, lint, `nuxt typecheck`, knip, 49 web unit tests across ten files); `pnpm test:e2e` still passes its 19 route tests. No new dependencies. Port 3000 was held by an unrelated process on the dev machine, so the manual check ran on 3077.
- `BottomSheet.vue` (`app/components/`): custom, SSR-rendered `<aside data-testid="bottom-sheet" data-snap="peek|half|full">`. `v-model:snap` (`SheetSnap` from `app/utils/sheet.ts`, default `peek`), prop `label` (aria-label, default "Houses and neighbourhood"). Snap heights are CSS: peek `max(96px, 15dvh)`, half `50dvh`, full `90dvh`, `transition: height 250ms` and `0ms` under `prefers-reduced-motion`. The handle is a `<button data-testid="sheet-handle" aria-label="Resize panel (now half)">` with `touch-action: none`: pointer drag resizes (nearest snap on release, the trailing click is swallowed), tap cycles peek -> half -> full -> peek, ArrowUp/ArrowDown step, Home/End jump to full/peek; Escape anywhere inside the sheet collapses full -> half. Slot `card` renders above the scrollable body (`overscroll-behavior: contain`); the default slot is the body. At `>= 840px` a media query turns it into a fixed side panel one third wide, full height, handle hidden, no snapping. `padding-bottom: env(safe-area-inset-bottom)`. Pure helpers `cycleSnap`, `stepSnap`, `nearestSnap`, `snapHeightsPx` live in `app/utils/sheet.ts`. Not done: history Back collapsing the full state (ADR-0006 mentions Back or Escape); pushing a history entry from the sheet would fight `router.replace` in `useLocation`, so it is left for ticket 09/12 to decide.
- Composables (`app/composables/`): `useHouses(location, radius)` -> `{ key, result, houses, truncated, cap, status, error, upstreamError, refresh }`; `useFacts(location)` -> `{ key, result, facts, countryCode, status, error, upstreamError, refresh }`; `usePrices(houses)` -> `{ key, result, signals, byHouseId, status, error, upstreamError, refresh }`. All three use `useFetch` with a reactive key (`houses:52.3676:4.9041:500`, `facts:52.3676:4.9041`, `prices:<fnv1a digest of sorted house ids>`; `…:none` while disabled), `enabled` gating on a Location / a non-empty house list, `server: false` + `lazy: true` (the SSR shell never waits on Overpass; the skeletons hydrate and fill), and `getCachedData` = `cachedApiData` from `app/utils/api.ts`, which returns `nuxtApp.payload.data[key] ?? nuxtApp.static.data[key]` except for `refresh:manual` (the retry). A custom `getCachedData` also switches off Nuxt's `purgeCachedData`, so every area visited stays cached for the session (user story 40). Do not pass `watch: false` to these `useFetch` calls: in Nuxt 4.5 that sets `_keyTriggersExecute = false` and a key change no longer refetches. `upstreamErrorOf(error)` narrows `FetchError.data.error` with `isUpstreamError`.
- `useLocaleFormat(countryCode)` -> `{ locale, distanceSystem, price(amount, currency), priceAbbreviated(amount, currency), distance(metres) }`, wrapping `localeForCountryCode`, `formatPrice`, `formatPriceAbbreviated`. `formatDistance` / `distanceSystemFor` are exported pure helpers: metric `450 m` / `1.7 km`; `US` feet under 0.2 mi then miles; `GB` metres under half a mile then miles (`Intl.NumberFormat` unit style). Ticket 10 should feed it `facts.hierarchy.countryCode`.
- `ErrorRetry.vue`: props `service?`, `retrying?`; emits `retry`; `role="alert"`, `data-testid="error-retry"`, copy "The open-data service is unavailable right now. It's them, not you." The page shows it at the top of the sheet when any of houses, facts or prices carries an upstream error, snaps the sheet to half so the button is on screen, and retry refreshes only the failed ones.
- `index.vue`: no Location -> `LocationPrompt` (unchanged layout). With one: fixed header (`--one-pager-header-height: 3.5rem` under `env(safe-area-inset-top)`; `h1`, search toggle `data-testid="search-toggle"` that opens a `PlaceSearch` panel below the bar, Escape or a selection closes it, `LocateMeButton`), `HouseMapFrame` at `100dvh` with `--house-map-top-offset: calc(3.5rem + 16px)`, `peek-height="var(--sheet-peek)"` where `.one-pager` sets `--sheet-peek: max(96px, 15dvh)` and `0px` at `>= 840px` (so no JS media query), `test-hook` from `runtimeConfig.public.testHooks` (`NUXT_PUBLIC_TEST_HOOKS=true` for ticket 12), the map's `locate` slot filled with a hidden span because the header owns the locate control. Map `select` writes `h=` and lifts the sheet from peek to half; `search-here` -> `setLocation`; `radius-change` -> `setRadius`. `MapHouse.priceSignal` is `undefined` until prices load, then the signal or `null`. Sections: `#houses` (48 px heading row with "122 houses, 64 with prices", four skeleton rows while loading, `data-testid="houses-skeleton"`) and `#neighbourhood` (heading row with the Neighbourhood name, four skeleton tiles, `data-testid="neighbourhood-skeleton"`). `useSeoMeta` sets title and description; `nuxt.config.ts` sets the viewport meta with `viewport-fit=cover`.
- `useLocation.ts` now imports `RADIUS_OPTIONS` from `app/utils/map/radius.ts` and re-exports it (ticket 07 request).
- `PlaceSearch.vue` follow-up: no more per-keystroke fetch. It is a `<form role="search">` with a 48 px submit button (`data-testid="place-search-submit"`); Enter or the button searches, editing the query drops the results, refocusing shows the last results again. The `debounceMs` prop is gone. Test updated (five cases).
- Manual check with Playwright (headless Chromium, 2x): phone 390x844 with the fixture provider showed the header (57 px), the map at `ready` in ~2.5 s with seven clusters, the radius group and attribution 16 px above the sheet, and the sheet at peek (126.6 px = 15dvh) with the handle and "Houses / 122 houses, 64 with prices"; `__houseMap.getState()` reported 122 houses. 1280x800 showed the map 853 px wide with the zoom control under the header and the side panel 427 px wide, full height, with both sections. With `NUXT_DATA_PROVIDER=failing` all three routes returned 502, the map still rendered its circle with zero houses, and the sheet showed the retry card with "Service: failing-provider" (then this ticket added the snap to half). No console errors beyond the 502s and Chromium's WebGL ReadPixels warning.
