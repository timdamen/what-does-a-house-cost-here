/**
 * Domain package: types, the Data Provider port, geo helpers and the fixture provider.
 * Populated by later tickets. This placeholder keeps the workspace importable.
 */
export const DOMAIN_PACKAGE_NAME = '@house-cost/domain';
